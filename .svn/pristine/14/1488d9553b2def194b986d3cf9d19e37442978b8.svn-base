import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LotacoesExcluirComponent } from './lotacoes-excluir.component';

describe('LotacoesExcluirComponent', () => {
  let component: LotacoesExcluirComponent;
  let fixture: ComponentFixture<LotacoesExcluirComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LotacoesExcluirComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LotacoesExcluirComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
