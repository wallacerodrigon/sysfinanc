import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LotacoesListComponent } from './lotacoes-list.component';

describe('LotacoesListComponent', () => {
  let component: LotacoesListComponent;
  let fixture: ComponentFixture<LotacoesListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LotacoesListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LotacoesListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
