import { AppPage } from './app.po';

describe('E-Social APP', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  it('espera receber o titulo do sistema', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('E-Social');
  });
});
