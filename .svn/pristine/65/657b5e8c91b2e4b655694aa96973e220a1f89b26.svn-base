.ebonyClayAutoContraste {
  // Tema padrão
  $app-primary: mat-palette($app-base-color);
  $app-accent: mat-palette($app-base-color, A200, A100, A400);
  $app-warn: mat-palette($app-warn-color);
  $app-theme: mat-dark-theme(
      $app-primary,
      $app-accent,
      $app-warn);
  @include angular-material-theme($app-theme);

  // Componente toolbar
  $app-toolbarprimary: mat-palette($app-base-color);
  $app-toolbaraccent: mat-palette($mat-blue-grey, 900, A400);
  $app-toolbarwarn: mat-palette($mat-red);
  $app-toolbartheme: mat-dark-theme(
      $app-toolbarprimary,
      $app-toolbaraccent,
      $app-toolbarwarn);

  @include mat-toolbar-theme($app-toolbartheme);

  // Sidebar
  .mat-sidenav {
    min-height: 100%;
    background-color: #151515;
    .app-sidebar-header {
      height: 150px;
      border-bottom: 1px solid #313A4A;
      img {
        margin-top: 20px;
        padding: 0 24px;
      }

      .unidade {
        margin-top: 20px;
        padding: 0 33px;
        color: #FFFFFF;
        font-size: 15px;
      }

      .sistema {
        margin-top: 10px;
        padding: 0 33px;

        color: #fff;
        font-size: 14px;
      }
    }
    .mat-expansion-panel {
      background: transparent;
      padding: 0;
      border-bottom: 1px solid #313A4A;
      @include mat-elevation(0);
      .mat-nav-list {
        border-top: 1px solid #313A4A;
      }
      .mat-list-item-content, .mat-expansion-panel-header {
        padding: 0px 16px 0 32px;
      }
      .mat-expansion-panel-header-title,
      .mat-expansion-indicator,
      .mat-expansion-indicator::after {
        color: #B6B6B6;
      }
      .mat-expansion-panel-body {
        padding: 0px !important;
      }
    }
    .app-sidebar-container {
      min-height: 100%;
      .mat-nav-list {
        padding: 0px;
      }
      .mat-list-item {
        border-bottom: 1px solid #313A4A;
        &.active {
          background: rgba(0, 0, 0, 0.25);
          a {
            color: #FFFFFF;
          }
          .mat-icon {
            color: #FFFFFF;
          }
        }
        .mat-icon {
          margin: 0 15px;
          color: #d2d4d7;
          font-size: 16px;
        }
        a {
          color: #d2d4d7;
          font-size: 14px;
        }
      }
    }
  }
}
