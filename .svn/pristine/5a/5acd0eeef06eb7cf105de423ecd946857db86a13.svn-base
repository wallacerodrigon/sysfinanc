import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RubricaPersistComponent } from './rubrica-persist.component';

describe('RubricaPersistComponent', () => {
  let component: RubricaPersistComponent;
  let fixture: ComponentFixture<RubricaPersistComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RubricaPersistComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RubricaPersistComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
