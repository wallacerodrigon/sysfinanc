import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LotacoesIncluirComponent } from './lotacoes-incluir.component';

describe('LotacoesIncluirComponent', () => {
  let component: LotacoesIncluirComponent;
  let fixture: ComponentFixture<LotacoesIncluirComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LotacoesIncluirComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LotacoesIncluirComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
