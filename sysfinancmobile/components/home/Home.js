import {Text} from 'react-native';

import axios from 'axios';

const Home = () => {
    return (
        <Text>Home</Text>
    )
}

//axios.get(url)
//retorna promise --> then(resp => resp.data)

export default Home;